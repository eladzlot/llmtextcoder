% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pii.R
\name{scan_pii}
\alias{scan_pii}
\title{Scan a data frame of texts for potentially identifying information}
\usage{
scan_pii(
  df,
  auto_patterns = auto_pii_patterns(),
  disclosure_patterns = disclosure_pii_patterns()
)
}
\arguments{
\item{df}{A data frame with at minimum columns \code{id} and \code{text}.}

\item{auto_patterns}{Named character vector of auto-redactable patterns.
Default: \code{\link[=auto_pii_patterns]{auto_pii_patterns()}}.}

\item{disclosure_patterns}{Named character vector of disclosure patterns.
Default: \code{\link[=disclosure_pii_patterns]{disclosure_pii_patterns()}}.}
}
\value{
A \code{pii_scan} data frame with columns \code{id}, \code{pattern}, \code{tier}
(\code{"auto"} or \code{"disclosure"}), \code{occurrence} (integer, 1-based per
id–pattern pair), and \code{match} (trigger plus trailing context).
}
\description{
Searches each text against two sets of patterns:
\itemize{
\item \strong{Auto} patterns (emails, phones, etc.) capture the full identifying
value and can be cleared with \code{\link[=redact_pii]{redact_pii()}}.
\item \strong{Disclosure} patterns (name and location phrases) capture only a trigger
phrase; the words that follow require per-row human judgment via
\code{\link[=redact_words]{redact_words()}}.
}
}
\details{
The printed output separates the two tiers and generates ready-to-fill
\code{\link[=redact_words]{redact_words()}} calls for all disclosure matches, with \verb{n_words = ?} as
the only blank to complete.
}
\examples{
df <- data.frame(
  id   = c("P01", "P02"),
  text = c("Contact me at jane@example.com",
           "My name is Sarah and I live in London"),
  stringsAsFactors = FALSE
)
scan_pii(df)
}

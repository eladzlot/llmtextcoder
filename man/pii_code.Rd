% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pii.R
\name{pii_code}
\alias{pii_code}
\title{Extract suggested redaction and approval code from a PII scan}
\usage{
pii_code(x)
}
\arguments{
\item{x}{A \code{pii_scan} object returned by \code{\link[=scan_pii]{scan_pii()}}.}
}
\value{
A character string of R code, one call per line. Returns \code{""}
invisibly if there are no disclosure findings.
}
\description{
Returns the suggested \code{redact_words()} and \code{pii_approve()} calls from a
\code{\link[=scan_pii]{scan_pii()}} result as a single character string. Useful for copying the
code to your script programmatically rather than transcribing from the
printed output.
}
\examples{
df <- data.frame(
  id   = "P01",
  text = "My name is Sarah and I live in London.",
  stringsAsFactors = FALSE
)
result <- scan_pii(df)
code   <- pii_code(result)
cat(code)
# Copy to clipboard (requires the clipr package):
# clipr::write_clip(code)
}
\seealso{
\code{\link[=scan_pii]{scan_pii()}}, \code{\link[=redact_words]{redact_words()}}, \code{\link[=pii_approve]{pii_approve()}}
}

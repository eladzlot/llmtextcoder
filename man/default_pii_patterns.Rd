% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pii.R
\name{default_pii_patterns}
\alias{default_pii_patterns}
\title{All default PII patterns}
\usage{
default_pii_patterns()
}
\value{
A named character vector of Perl-compatible regex patterns.
}
\description{
Returns the union of \code{\link[=auto_pii_patterns]{auto_pii_patterns()}} and \code{\link[=disclosure_pii_patterns]{disclosure_pii_patterns()}}.
Used by \code{\link[=scan_pii]{scan_pii()}} to scan for all pattern types at once.
}
\details{
To customise, modify the two constituent sets and pass them separately to
\code{\link[=scan_pii]{scan_pii()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{auto <- auto_pii_patterns()
auto["national_id"] <- "\\\\b\\\\d\{9\}\\\\b"

disc <- disclosure_pii_patterns()
disc <- disc[names(disc) != "location_phrase"]

scan_pii(df, auto_patterns = auto, disclosure_patterns = disc)
}\if{html}{\out{</div>}}
}

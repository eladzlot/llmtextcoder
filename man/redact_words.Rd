% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pii.R
\name{redact_words}
\alias{redact_words}
\title{Redact a disclosure phrase and the words that follow it in a single row}
\usage{
redact_words(
  df,
  id,
  pattern,
  n_words = 3,
  occurrence = NULL,
  patterns = disclosure_pii_patterns()
)
}
\arguments{
\item{df}{A data frame with at minimum columns \code{id} and \code{text}.}

\item{id}{The participant identifier. Must match exactly one row.}

\item{pattern}{Name of the pattern to redact (must be a key in \code{patterns}).}

\item{n_words}{Number of words to consume after the trigger phrase.}

\item{occurrence}{Integer. Which occurrence to target when the pattern fires
multiple times. \code{NULL} (default) replaces all occurrences.}

\item{patterns}{Named character vector of patterns. Default:
\code{\link[=disclosure_pii_patterns]{disclosure_pii_patterns()}}.}
}
\value{
A copy of \code{df} with the targeted text replaced in \code{text}.
}
\description{
For patterns that match only a trigger phrase ("my name is", "I live in"),
this function finds the match in one specific row and replaces the trigger
plus \code{n_words} following words with a placeholder. Use after reviewing the
output of \code{\link[=scan_pii]{scan_pii()}}.
}
\details{
The \code{occurrence} argument targets a specific match when a pattern fires
more than once in the same text. When \code{occurrence} is \code{NULL} (the default),
all occurrences are replaced with the same \code{n_words}.
}
\examples{
df <- data.frame(
  id   = "P01",
  text = "It was hard. My name is Sarah Johnson and I felt lost.",
  stringsAsFactors = FALSE
)
redact_words(df, id = "P01", pattern = "name_disclosure", n_words = 2)
}

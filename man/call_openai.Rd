% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{call_openai}
\alias{call_openai}
\title{Call the OpenAI chat-completions endpoint}
\usage{
call_openai(
  prompt,
  params = run_params(),
  api_key = Sys.getenv("OPENAI_API_KEY"),
  base_url = "https://api.openai.com/v1"
)
}
\arguments{
\item{prompt}{Character scalar. The fully assembled prompt — typically the
output of \code{\link[=build_prompt]{build_prompt()}}.}

\item{params}{A \code{run_params} object created by \code{\link[=run_params]{run_params()}}.}

\item{api_key}{Character. OpenAI API key. Default reads the
\code{OPENAI_API_KEY} environment variable. Store your key in a gitignored
\code{.env} file and load it with \code{readRenviron(".env")}.}

\item{base_url}{Character. Base URL for the API. Override for non-default
endpoints (e.g. Azure OpenAI deployments).}
}
\value{
Character scalar: the raw JSON string returned by the model.
}
\description{
Sends a single user message and returns the raw JSON text of the model's
reply. Forces structured JSON output via \code{response_format}, so the reply
is always a valid JSON string ready for \code{\link[=parse_response]{parse_response()}}. The rubric
template must instruct the model to respond in JSON (OpenAI's requirement
for this mode).
}
\details{
Transient failures (429 rate-limit, 500/502/503 server errors) are retried
up to three times with exponential backoff before an error is raised.
}
\examples{
\dontrun{
readRenviron(".env")
template <- read_template("prompts/rubric_v1.txt")
raw      <- call_openai(build_prompt(template, list(text = "Some text.")), run_params())
}
}

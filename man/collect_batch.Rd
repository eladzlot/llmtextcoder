% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{collect_batch}
\alias{collect_batch}
\title{Collect results from a pending OpenAI Batch API job}
\usage{
collect_batch(
  template_path,
  params = run_params(),
  output_dir = "data",
  api_key = Sys.getenv("OPENAI_API_KEY"),
  base_url = "https://api.openai.com/v1"
)
}
\arguments{
\item{template_path}{Character scalar. Path to the \code{.txt} rubric template.
Must match what was passed to \code{\link[=submit_batch]{submit_batch()}}.}

\item{params}{A \code{run_params} object. Must match what was passed to
\code{\link[=submit_batch]{submit_batch()}}.}

\item{output_dir}{Character scalar. Must match what was passed to
\code{\link[=submit_batch]{submit_batch()}}. Default \code{"data"}.}

\item{api_key}{Character. OpenAI API key.}

\item{base_url}{Character. OpenAI API base URL.}
}
\value{
The output CSV path invisibly if the batch completed, \code{NULL} if
still in progress.
}
\description{
Checks the status of the batch submitted by \code{\link[=submit_batch]{submit_batch()}}. If complete,
results are written to the same output CSV as \code{\link[=score_many]{score_many()}} and the state
file is removed. If still in progress, the current counts are printed and
the function returns \code{NULL} — call again later.
}
\details{
Failures are written to \verb{_errors.csv} and the run continues, consistent
with \code{\link[=score_many]{score_many()}} behaviour.
}
\examples{
\dontrun{
collect_batch("rubric_v1.txt", run_params(), output_dir = "results")
}
}

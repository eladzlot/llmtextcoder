% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{submit_batch}
\alias{submit_batch}
\title{Submit a batch scoring job to the OpenAI Batch API}
\usage{
submit_batch(
  df,
  template_path,
  params = run_params(),
  n = Inf,
  output_dir = "data",
  completion_window = "24h",
  api_key = Sys.getenv("OPENAI_API_KEY"),
  base_url = "https://api.openai.com/v1"
)
}
\arguments{
\item{df}{A data frame with column \code{id} and one column per \code{{{placeholder}}}
in the template.}

\item{template_path}{Character scalar. Path to the \code{.txt} rubric template.}

\item{params}{A \code{run_params} object (from \code{\link[=run_params]{run_params()}}).}

\item{n}{Integer. Maximum number of pending rows to include. Default \code{Inf}.}

\item{output_dir}{Character scalar. Directory for output files. Default
\code{"data"}.}

\item{completion_window}{Character. \code{"24h"} (default) or \code{"1h"}.}

\item{api_key}{Character. OpenAI API key.}

\item{base_url}{Character. OpenAI API base URL.}
}
\value{
The batch ID string, invisibly.
}
\description{
Uploads all pending rows (those not already in the output CSV) as a single
asynchronous batch job. Results are available within 24 hours at 50\% of the
standard per-token cost. Use \code{\link[=collect_batch]{collect_batch()}} to retrieve results once the
job completes.
}
\details{
A state file (\verb{<stem>_batch_<model>.json}) is written to \code{output_dir}
recording the batch ID, submitted row IDs, and original data values. This
file is required by \code{\link[=collect_batch]{collect_batch()}} and is removed automatically when
results are collected. If a state file already exists, \code{submit_batch()}
errors — collect the pending batch first.

The template determines which columns of \code{df} are required: every
\code{{{placeholder}}} must correspond to a column in \code{df}.
}
\section{Multiple concurrent batches}{

Each rubric–model combination has its own state file, so batches for
different rubrics or different models can run simultaneously without
interference.
}

\examples{
\dontrun{
readRenviron(".env")
df     <- read.csv("participants.csv")
params <- run_params(model = "gpt-4o")

submit_batch(df, "rubric_v1.txt", params, output_dir = "results")
# Close R, come back in up to 24 hours:
collect_batch("rubric_v1.txt", params, output_dir = "results")
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pii.R
\name{redact_pii}
\alias{redact_pii}
\title{Redact structural PII across all texts}
\usage{
redact_pii(df, patterns = auto_pii_patterns())
}
\arguments{
\item{df}{A data frame with at minimum columns \code{id} and \code{text}.}

\item{patterns}{Named character vector of patterns to redact. Default:
\code{\link[=auto_pii_patterns]{auto_pii_patterns()}}.}
}
\value{
A copy of \code{df} with matching strings replaced in \code{text}.
}
\description{
Replaces every match of each pattern in \code{patterns} with a labelled
placeholder such as \verb{[email redacted]}. Replacement is done by regex —
the original values never appear in your code.
}
\details{
Defaults to \code{\link[=auto_pii_patterns]{auto_pii_patterns()}} (emails, phones, Prolific IDs, etc.),
which capture the full identifying value and are safe to replace globally.
Disclosure patterns (name phrases, location phrases) are intentionally
excluded from the default; handle those with \code{\link[=redact_words]{redact_words()}}.
}
\examples{
df <- data.frame(
  id   = "P01",
  text = "Email me at jane@example.com or call 07911 123456.",
  stringsAsFactors = FALSE
)
redact_pii(df)
}
